# Local Postgres (docker-compose maps host port 5433)
DATABASE_URL="postgresql://northcraft:northcraft@localhost:55432/northcraft"

# Single shared password for /admin (no full auth — iframe demo friendly)
ADMIN_PASSWORD="changeme-strong-admin-secret"

# Optional: set for production cookie Secure flag alignment
# NEXT_PUBLIC_APP_URL="http://localhost:3000"
